#include <stdio.h>
int main(){
    int x1, x2, x3, x4, x5;
    scanf("%d %d %d %d %d", &x1, &x2, &x3, &x4, &x5);
    int sum = x1 + x2 + x3 + x4 + x5;
    printf("The summation is %d\n", sum);
    return 0;
}
