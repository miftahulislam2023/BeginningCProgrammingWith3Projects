//write a program to input two numbers and output them on the screen
#include <stdio.h>
int main(){
    int x, y;
    printf("Enter two numbers: ");
    scanf("%d %d", &x, &y);
    printf("%d %d", x, y);
    return 0;
}
